<% if item.rating.present? %>
  <%= link_to "感想を編集", url, class: "inline-flex items-center rounded-full border border-slate-300 px-3 py-1.5 text-sm font-semibold text-slate-600 transition hover:bg-slate-50" %>
<% else %>
  <%= link_to "感想を書く", url, class: "inline-flex items-center rounded-full bg-emerald-100 px-3 py-1.5 text-sm font-semibold text-emerald-800 transition hover:bg-emerald-200" %>
<% end %>
